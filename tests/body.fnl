(when true
  (print "you can't handle the truth!")
  (os.exit 1))

;; what

(fn abc []
  (do (set fail (+ fail 1))
      (print "FAIL")
      ;; hey
      (print "Expected:")
      (print after)
      (print "Got:")
      (print actual)))
